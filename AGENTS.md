# AGENTS.md

The working agreement for this repo lives in [`CLAUDE.md`](./CLAUDE.md) — read it before changing
anything. This file exists so agents that look for `AGENTS.md` find it too; there is one set of
rules, not two, so nothing is restated here.

How **this checkout** is deployed on its two hosts (supervisord, not `collie restart`) lives in
[`AGENTS.local.md`](./AGENTS.local.md).

@CLAUDE.md
@AGENTS.local.md
